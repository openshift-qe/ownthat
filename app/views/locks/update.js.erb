<%  @lock = @lock.get_by_resource if !@lock.id && @persisted %>

<%= render(:partial => "error") %>

<% if @lock.valid? && @persisted %>
  <%= smart_listing_item :locks, :update, @lock, "locks/lock" %>
  <%#  (@lock.valid? && @persisted) ? "locks/lock" : "locks/form" %>
<% end %>
